import { IApplicationConfig } from "./interfaces/config";
const config: IApplicationConfig[] = [
  {
    name: "sparow",
    auth_token: "test-token",
    base_url: "core.sparow.salimon.ir",
  },
];
export default config;
