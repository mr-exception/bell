import { IApplicationConfig } from "./interfaces/config";
const config: IApplicationConfig[] = [
  // sparow application
  {
    name: "sparow",
    auth_token: "test-auth-token",
    base_url: "http://sparow_core/api",
  },
];

export default config;
